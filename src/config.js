const config = {
    // Frontend-specific configuration
    APP_NAME: 'SionDrop',
    theme: {
        colors: {
            primary: '#FFD100',    // Bright yellow
            secondary: '#000000',  // Black
            white: '#FFFFFF',      // White
            gray: '#F5F5F5',      // Light gray for backgrounds
            yellow: {
                light: '#FFE145',  // Light yellow for hover states
                dark: '#E5BC00'    // Darker yellow for active states
            }
        }
    }
};

export default config; 