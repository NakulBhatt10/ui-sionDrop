import React from 'react';

const ModeSelection = () => {
    return (
        <div className="mode-selection-container">
            <h2>Select Your Mode</h2>
            <div className="modes-grid">
                {/* Add different mode options here */}
            </div>
        </div>
    );
};

export default ModeSelection; 